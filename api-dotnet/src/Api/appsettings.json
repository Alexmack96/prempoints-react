{
  "AllowedOrigins": [
    "http://localhost:5173", // Vite local
    "https://my-production-app.com" // Live URL
  ],
  // ConnectionStrings:PremPoints is intentionally absent — it carries the
  // PremPointsDev SQL login's password, so it lives in user-secrets:
  //
  //   dotnet user-secrets set "ConnectionStrings:PremPoints" "<string>" --project src/Api
  //
  // Needed here for the standalone launch profiles and for `dotnet ef`, which
  // builds this host at design time. Under Aspire the AppHost overrides it with
  // the ConnectionStrings__PremPoints environment variable.
  "AppSettings": {
    "TradingWindow": {
      "Open": "09:30",
      "Close": "16:00",
      "TimeZone": "America/New_York"
    }
  },
  "WorkOS": {
    "ClientId": "client_01KBQJ6BCKS24TJF4RG5GGBCA7",
    "DiscoveryEndpoint" :  ""
  }
}